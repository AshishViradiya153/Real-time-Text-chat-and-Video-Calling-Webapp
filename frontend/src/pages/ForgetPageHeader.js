import React from "react";
import Typography from "@mui/material/Typography";

const ForgetPageHeader = () => {
  return (
    <>
      <Typography variant="h5" style={{ color: "#eff6ff" }}>
        Don't worry!
      </Typography>
      <Typography style={{ color: "#e5f0ff" }}>
        Add your email and new password for change password
      </Typography>
    </>
  );
};

export default ForgetPageHeader;
